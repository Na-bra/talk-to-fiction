{
  "name": "ai-npc-generator-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm --prefix server install",
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server start",
    "test": "npm --prefix server run test",
    "models": "npm --prefix server run models"
  },
  "engines": {
    "node": ">=20"
  },
  "description": "API for the AI NPC Generator — persistent fictional characters with memory, secrets and relationships.",
  "dependencies": {
    "@supabase/server": "^1.6.0"
  }
}
